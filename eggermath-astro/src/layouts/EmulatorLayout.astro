---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  canonical?: string;
  jsonLd?: object;
  hreflangHtml?: string;
  noRedirectAds?: boolean;
}

const { title, description, ogImage, canonical, jsonLd, hreflangHtml, noRedirectAds = false } = Astro.props;
---
<BaseLayout title={title} description={description} ogImage={ogImage} canonical={canonical} jsonLd={jsonLd} hreflangHtml={hreflangHtml} noRedirectAds={noRedirectAds}>
  <slot />
</BaseLayout>

<script>
  // Detect embed mode
  (function() {
    var params = new URLSearchParams(window.location.search);
    if (params.get('game') || (window.self !== window.top)) {
      document.body.classList.add('embed-mode');
    }
  })();
</script>
